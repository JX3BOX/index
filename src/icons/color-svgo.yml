# replace default config

# multipass: true
# full: true

plugins:
    # - name
    #
    # or:
    # - name: false
    # - name: true
    #
    # or:
    # - name:
    #     param1: 1
    #     param2: 2

    # remove fill, only use svg without complex colors
    # the purpose is to inherit the color of the parent

    #- removeAttrs:
    #      attrs:
    #          - "fill"
    #          - "fill-rule"
